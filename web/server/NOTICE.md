# Port scope

The web layer is a browser implementation of SideStore's app-management workflow. Native iOS-only components such as LocalDevVPN, EM Proxy, and iOS private frameworks are not executable in Safari and therefore remain outside the PWA runtime.
